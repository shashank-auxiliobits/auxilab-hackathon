## Summary

What does this change and why?

## Changes

-

## Checklist

- [ ] `make lint` and `make format` pass
- [ ] `make typecheck` passes (mypy strict)
- [ ] `make test` (unit) passes
- [ ] `make test-int` (integration) passes
- [ ] Added/updated tests for the change
- [ ] Updated docs (`docs/`, README) if behaviour changed
- [ ] Added an Alembic migration if models changed (`alembic check` clean)
